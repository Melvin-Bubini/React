export interface Product {
    id: number,
    category: string,
    name: string,
    description: string,
    price: number
}

export interface ProductCredentials {
    category: string,
    name: string,
    description: string,
    price: number
}